\section{问题三：两种代表工况策略与调节优先级}

\subsection{代表工况选择}

以入口质量负荷 $C_{\mathrm{in}}Q$ 为主轴，在样本量充足的稳定状态中选择 R2 与 R8。R2 的平均入口浓度为 $25.843\gNm$、质量负荷为 $1.232\times10^7$；R8 的平均入口浓度为 $46.090\gNm$、质量负荷为 $2.217\times10^7$，后者高约 79.9\%。二者中位驻留时间分别为 852 min 与 870 min，排除了极短异常段造成的偶然差异。

\subsection{可执行参数表}

表\ref{tab:q3u} 和表\ref{tab:q3t} 将最优参数与该工况历史均值并列。其目的不是要求现场一步跳到目标值，而是给出工况稳定后可采用的设定中心；实际控制应增加斜率限制和切换滞回。

\begin{table}[H]
\centering
\caption{低、高负荷代表工况的电压策略}
\label{tab:q3u}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ccrrrrrrrr}
\toprule
工况 & 标签 & $U_1^*$ & 历史 $U_1$ & $U_2^*$ & 历史 $U_2$ & $U_3^*$ & 历史 $U_3$ & $U_4^*$ & 历史 $U_4$\\
\midrule
低负荷 & R2 & 70.900 & 60.496 & 68.147 & 60.522 & 59.200 & 51.338 & 44.743 & 51.323\\
高负荷 & R8 & 70.900 & 62.055 & 70.447 & 62.125 & 59.200 & 44.572 & 53.314 & 44.633\\
\bottomrule
\end{tabular}}
\end{table}

\begin{table}[H]
\centering
\caption{低、高负荷代表工况的振打与风险指标}
\label{tab:q3t}
\resizebox{\textwidth}{!}{%
\begin{tabular}{ccrrrrrrrrrr}
\toprule
工况 & $T_1^*$ & 历史 $T_1$ & $T_2^*$ & 历史 $T_2$ & $T_3^*$ & 历史 $T_3$ & $T_4^*$ & 历史 $T_4$ & $P^*$ & $q_{.95}(C)$\\
\midrule
R2 & 290.0 & 247.7 & 289.0 & 247.6 & 506.0 & 443.9 & 510.0 & 443.5 & 1947.6 & 9.052\\
R8 & 245.4 & 195.9 & 256.3 & 195.4 & 414.4 & 440.6 & 463.8 & 440.4 & 2138.2 & 9.209\\
\bottomrule
\end{tabular}}
\end{table}

低负荷 R2 的尘负荷指数仅 1.098，四场周期均可延长以降低振打频率；高负荷 R8 的指数达到 1.45，上游两场周期虽较历史均值延长，但明显短于 R2，第三场周期还需由 440.6 s 缩短至 414.4 s。电压方面，高负荷状态需要同时强化前三级并提高末级余量；低负荷状态则可降低第四场电压，将功率集中在单位电耗收益更高的电场。

\subsection{边际收益与优先级切换}

图\ref{fig:priority} 展示式\eqref{eq:priority} 的反事实结果。两工况的纯减排效率排序都以 $U_3$、$U_1$ 居前，电压项显著高于周期项。这并不意味着振打永远不重要：周期主要承担尘负荷和瞬时峰值安全约束，其价值不能只由稳态浓度斜率衡量。

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{fig10_priority_comparison.png}
\caption{低、高负荷工况的单位新增电耗减排收益}
\label{fig:priority}
\end{figure}

低负荷 R2 中，$U_3,U_1,U_4,U_2$ 的收益依次为 0.0666、0.0620、0.0523、0.0486，单位均为 $\mathrm{(mg/Nm^3)/kW}$；最高的周期收益 $T_1$ 为 0.0129。高负荷 R8 中，$U_3$ 与 $U_1$ 均约为 0.0627，$U_4$ 上升至 0.0558，说明末级精细捕集的重要性提高；周期项直接收益较小，但尘负荷约束已经活跃。

据此形成表\ref{tab:rules} 的优先级规则。

\begin{table}[H]
\centering
\caption{工况—风险—优先动作控制规则}
\label{tab:rules}
\begin{tabularx}{0.96\textwidth}{C{3.0cm}C{3.2cm}L L}
\toprule
识别信号 & 主导风险 & 第一动作 & 第二动作\\
\midrule
低/中负荷，$I_M<1.30$ & 基线穿透 & 按 $E_j$ 提高高收益电场电压 & 在风险裕量内延长周期节能\\
高负荷，$I_M\rightarrow1.45$ & 积灰与峰值 & 缩短活跃电场周期并错峰 & 再提高 $U_3,U_1$，保留 $U_4$ 余量\\
高温异常 & 迁移能力下降、反电晕不确定 & 前后场重分配，检查火花率 & 避免仅靠单场堆高电压\\
工况切换边界 & 频繁切换与过冲 & 入口质量负荷前馈+双阈值滞回 & 电压斜率限制与周期缓变\\
短时超限 & 多场峰值叠加 & 立即禁止同步振打 & 临时提高末级电压并进入应急模式\\
\bottomrule
\end{tabularx}
\end{table}

\subsection{现场切换逻辑}

建议以 $15$ 分钟稳健工况标签作为慢层，以分钟级入口质量负荷作为快层。进入高负荷状态的阈值应高于退出阈值，避免在聚类边界反复跳变；电压目标通过斜率限制逐步到达，振打周期只在一个完整振打循环结束后更新。若某场尘负荷风险高于阈值，则周期安全优先级高于电压经济性优先级。四场的振打触发相位错开，末级电场保留快速补偿通道。
